import std.math;

func main() {
    let x = -10;
    let abs_x = std.math.iabs(x);
    print(abs_x);

    let root = std.math.sqrt(16.0);
    print(root);

    let power = std.math.pow(2.0, 8.0);
    print(power);

    return 0;
}
