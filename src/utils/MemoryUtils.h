#ifndef MEADOWS_MEMORY_UTILS_H
#define MEADOWS_MEMORY_UTILS_H

#include <cstddef>

namespace meadows {

constexpr size_t MAX_ALLOC_SIZE = 10 * 1024 * 1024;
constexpr size_t MAX_STRING_LENGTH = 1024 * 1024;

} // namespace meadows

#endif // MEADOWS_MEMORY_UTILS_H
